preset: page
fields:
  link:
    pages:
      query: page.parent.children
      info: test
      text: '{{ page.slug }}'
      image:
        cover: true
    files:
      query: page.parent.files
      info: test
      text: '{{ file.filename }}'
  pages:
    query: page.parent.children
  files:
    query: page.parent.files
